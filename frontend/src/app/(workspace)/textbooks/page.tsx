import { BookOpen, ChevronRight, FolderTree, Plus } from 'lucide-react';
import { PageHeader, ProgressBar, StatusPill } from '@/components/mistake-atlas/ui';
import { textbooks } from '@/lib/mistake-atlas-data';

export default function TextbooksPage() {
  return <><PageHeader eyebrow="Taxonomy" title="教材与章节" description="知识点始终挂在教材章节下；停用或合并章节不会破坏历史错题。" action={<button className="atlas-button-primary"><Plus className="size-4" />新建教材</button>} /><div className="grid gap-5 lg:grid-cols-2">{textbooks.map((book, index) => <div key={book.name} className="atlas-card p-6"><div className="flex items-start gap-4"><div className={`grid size-12 place-items-center rounded-xl ${index === 0 ? 'bg-blue-50 text-blue-700' : 'bg-slate-100 text-slate-600'}`}><BookOpen className="size-6" /></div><div className="flex-1"><div className="flex items-center justify-between gap-3"><h2 className="font-serif text-lg font-semibold text-slate-900">{book.name}</h2><StatusPill tone={index === 0 ? 'blue' : 'slate'}>{index === 0 ? '主要教材' : '启用'}</StatusPill></div><p className="mt-1 text-xs text-slate-400">{book.chapters} 个章节 · {book.questions} 道错题</p></div></div><div className="mt-6"><div className="mb-2 flex justify-between text-xs"><span className="text-slate-500">错题掌握率</span><strong>{book.progress}%</strong></div><ProgressBar value={book.progress} tone={book.progress >= 60 ? 'green' : 'blue'} /></div><button className="mt-5 flex w-full items-center justify-between rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-50"><span className="flex items-center gap-2"><FolderTree className="size-4 text-slate-400" />管理章节树</span><ChevronRight className="size-4 text-slate-400" /></button></div>)}</div></>;
}
